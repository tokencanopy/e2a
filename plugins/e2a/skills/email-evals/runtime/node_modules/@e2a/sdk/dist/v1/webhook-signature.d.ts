import type { MessageLifecycleTransition as GeneratedMessageLifecycleTransition } from "./generated/models/MessageLifecycleTransition.js";
export interface VerifySignatureOptions {
    /** Raw HTTP request body bytes. */
    rawBody: string | Buffer;
    /** Value of the X-E2A-Signature header. */
    header: string;
    /** Webhook signing secret (whsec_...). Pass an array to verify one handler
     *  against several endpoints' secrets. */
    secret: string | string[];
    /** Tolerance in seconds; defaults to 300. */
    toleranceSeconds?: number;
    /** Test-only clock override; defaults to Date.now(). */
    now?: () => number;
}
/**
 * Verify an X-E2A-Signature header. Returns true on success, false on any
 * failure (bad format, missing pair, signature mismatch, replay). Never throws.
 */
export declare function verifyWebhookSignature(opts: VerifySignatureOptions): boolean;
/** A verified event envelope — the shape of a webhook delivery body, a
 *  `GET /v1/events/{id}` object, AND a WebSocket frame (all three channels
 *  share it). `data` is `unknown` at the envelope level so unknown/beta event
 *  types still parse; narrow on `type` with the `isEmail*` / `isDomain*`
 *  guards below to get the typed payload of a stable event. */
export interface WebhookEvent {
    id: string;
    type: string;
    /** Envelope schema version (currently "1"). Branch on it before parsing
     *  `data` if you need forward-compatibility across envelope revisions. */
    schema_version: string;
    created_at: string;
    data: unknown;
    [k: string]: unknown;
}
/** Metadata for one attachment (never the bytes). `index` is the stable
 *  0-based fetch key for `GET …/messages/{id}/attachments/{index}`. */
export interface AttachmentMetaView {
    filename?: string;
    content_type?: string;
    size_bytes: number;
    index: number;
}
export interface SPFResult {
    status: "pass" | "fail" | "none" | "neutral" | "softfail" | "temperror" | "permerror";
    domain: string | null;
    aligned: boolean | null;
    detail?: string;
}
export interface DKIMResult {
    status: "pass" | "fail" | "none" | "neutral" | "policy" | "temperror" | "permerror";
    domain: string | null;
    selector: string | null;
    aligned: boolean | null;
    detail?: string;
}
export interface DMARCResult {
    status: "pass" | "fail" | "none" | "temperror" | "permerror";
    domain: string | null;
    policy: "none" | "quarantine" | "reject" | null;
    aligned_by: Array<"spf" | "dkim">;
    detail?: string;
}
export interface Authentication {
    spf: SPFResult;
    dkim: DKIMResult[];
    dmarc: DMARCResult;
}
/** Snake-case wire representation carried by event envelopes. Field types,
 * including all closed vocabularies, derive from the generated REST model so
 * webhook/WebSocket consumers cannot drift into a second lifecycle taxonomy. */
export type EventMessageLifecycleTransition = {
    id: GeneratedMessageLifecycleTransition["id"];
    message_id: GeneratedMessageLifecycleTransition["messageId"];
    direction: GeneratedMessageLifecycleTransition["direction"];
    recipient?: GeneratedMessageLifecycleTransition["recipient"];
    stage: GeneratedMessageLifecycleTransition["stage"];
    outcome: GeneratedMessageLifecycleTransition["outcome"];
    reason_code: GeneratedMessageLifecycleTransition["reasonCode"];
    retryable: GeneratedMessageLifecycleTransition["retryable"];
    evidence: GeneratedMessageLifecycleTransition["evidence"];
    correlation_ids: GeneratedMessageLifecycleTransition["correlationIds"];
    occurred_at: string;
    reconstructed: GeneratedMessageLifecycleTransition["reconstructed"];
};
interface LifecycleTransitionCarrier {
    lifecycle_transitions?: EventMessageLifecycleTransition[];
}
/** Typed payload of an `email.received` event. The event is a metadata-only
 *  notification — it does NOT carry the message body. `message_id` + `delivered_to`
 *  are the fetch keys; pass the event to {@link E2AClient.webhooks.fetchMessage}
 *  (or call `client.messages.get(delivered_to, message_id)`) to retrieve the full
 *  message (body + attachment bytes). A non-null `verified_domain` means
 *  DMARC passed for that RFC 5322 From domain. */
export interface EmailReceivedData extends LifecycleTransitionCarrier {
    message_id: string;
    /** The receiving agent's email — its id and address (an agent's id IS its email). */
    agent_email: string;
    /** Always "inbound" on this event. */
    direction: string;
    conversation_id?: string;
    /** Parsed RFC 5322 From address; never replaced by Reply-To. */
    header_from: string | null;
    /** SMTP MAIL FROM; null for a null reverse path or providerless delivery. */
    envelope_from: string | null;
    /** RFC 5322 From domain when DMARC passed; null otherwise. */
    verified_domain: string | null;
    /** SMTP authentication evidence; null for providerless delivery. */
    authentication: Authentication | null;
    to: string[];
    cc: string[];
    reply_to: string[];
    /** The one agent address this per-agent copy was delivered to (scalar by
     *  construction — one event per delivery). The fetch key. */
    delivered_to: string;
    subject: string;
    received_at: string;
    /** Attachment METADATA (never bytes). Omitted when the message has none. */
    attachments?: AttachmentMetaView[];
}
/** Typed payload of an `email.sent` event — an outbound send reached its sent
 *  state through provider acceptance or atomic local loopback delivery. */
export interface EmailSentData extends LifecycleTransitionCarrier {
    message_id: string;
    agent_email: string;
    /** Always "outbound" on this event. */
    direction: string;
    conversation_id?: string;
    /** Provider-assigned (SES) id — the correlation key for the async
     *  delivered/bounced/complained feedback events. Absent for providerless
     *  local loopback delivery. */
    provider_message_id?: string;
    /** Open set; tolerate unknown values. Known values: smtp, loopback. */
    method: string;
    from: string;
    to: string[];
    cc?: string[];
    bcc?: string[];
    subject: string;
    /** Open set; tolerate unknown values. Known values: send, reply, forward. */
    message_type: string;
}
/** Typed payload of an `email.failed` event — an outbound send terminally
 *  failed (retries exhausted / permanent reject). */
export interface EmailFailedData extends LifecycleTransitionCarrier {
    message_id: string;
    agent_email: string;
    /** Always "outbound" on this event. */
    direction: string;
    conversation_id?: string;
    /** Open set; tolerate unknown values. Known values: smtp. */
    method: string;
    from: string;
    to: string[];
    cc?: string[];
    bcc?: string[];
    subject: string;
    /** Open set; tolerate unknown values. Known values: send, reply, forward. */
    message_type: string;
    /** Human-readable terminal failure diagnostic. */
    reason: string;
    /** Optional machine-readable failure code. */
    reason_code?: string;
    /** Whether re-submitting could succeed. Present only when the send path
     *  genuinely knows; absent ≠ false. */
    retryable?: boolean;
}
/** Typed payload of an `email.delivered` event — the recipient's server
 *  accepted an outbound message, per recipient. The event TYPE is the
 *  outcome; there is no `status` field. */
export interface EmailDeliveredData extends LifecycleTransitionCarrier {
    message_id: string;
    agent_email: string;
    /** Always "outbound" on this event. */
    direction: string;
    /** The one recipient address this per-recipient outcome is about. */
    delivered_to: string;
    subject?: string;
    /** Provider diagnostic (e.g. the remote SMTP response), when present. */
    smtp_detail?: string;
}
/** Typed payload of an `email.bounced` event — EmailDeliveredData's fields
 *  plus the SES bounce classification. */
export interface EmailBouncedData extends LifecycleTransitionCarrier {
    message_id: string;
    agent_email: string;
    /** Always "outbound" on this event. */
    direction: string;
    /** The one recipient address this per-recipient outcome is about. */
    delivered_to: string;
    subject?: string;
    smtp_detail?: string;
    /** Normalized SES bounce classification. Only a permanent (hard) bounce
     *  auto-suppresses the address. Deliberately a closed set: exhaustive after
     *  server-side normalization ("undetermined" is the guaranteed catch-all). */
    bounce_type: "permanent" | "transient" | "undetermined";
    /** Raw SES bounceSubType (e.g. General, NoEmail, MailboxFull). */
    bounce_sub_type?: string;
}
/** Typed payload of an `email.complained` event — a recipient marked an
 *  outbound message as spam. `smtp_detail` carries the complaint feedback
 *  type when present. */
export interface EmailComplainedData extends LifecycleTransitionCarrier {
    message_id: string;
    agent_email: string;
    /** Always "outbound" on this event. */
    direction: string;
    /** The one recipient address this per-recipient outcome is about. */
    delivered_to: string;
    subject?: string;
    smtp_detail?: string;
}
/** Typed payload of a `domain.sending_verified` event. */
export interface DomainSendingVerifiedData {
    domain: string;
    /** Open set; tolerate unknown values. Known values: verified. */
    sending_status: string;
}
/** Typed payload of a `domain.sending_failed` event. */
export interface DomainSendingFailedData {
    domain: string;
    /** Open set; tolerate unknown values. Known values: failed. */
    sending_status: string;
    reason?: string;
}
/** Typed payload of a `domain.suppression_added` event — an address was
 *  auto-suppressed after a hard bounce or complaint. Account-scoped despite
 *  the `domain.` prefix. */
export interface DomainSuppressionAddedData extends LifecycleTransitionCarrier {
    address: string;
    /** Open set; tolerate unknown values. Known values: bounce, complaint. */
    source: string;
    reason?: string;
    /** The outbound message whose feedback triggered the suppression, when known. */
    message_id?: string;
}
/** Narrow a verified event to `email.received` with typed `data`. */
export declare function isEmailReceived(e: WebhookEvent): e is WebhookEvent & {
    type: "email.received";
    data: EmailReceivedData;
};
/** Narrow a verified event to `email.sent` with typed `data`. */
export declare function isEmailSent(e: WebhookEvent): e is WebhookEvent & {
    type: "email.sent";
    data: EmailSentData;
};
/** Narrow a verified event to `email.failed` with typed `data`. */
export declare function isEmailFailed(e: WebhookEvent): e is WebhookEvent & {
    type: "email.failed";
    data: EmailFailedData;
};
/** Narrow a verified event to `email.delivered` with typed `data`. */
export declare function isEmailDelivered(e: WebhookEvent): e is WebhookEvent & {
    type: "email.delivered";
    data: EmailDeliveredData;
};
/** Narrow a verified event to `email.bounced` with typed `data`. */
export declare function isEmailBounced(e: WebhookEvent): e is WebhookEvent & {
    type: "email.bounced";
    data: EmailBouncedData;
};
/** Narrow a verified event to `email.complained` with typed `data`. */
export declare function isEmailComplained(e: WebhookEvent): e is WebhookEvent & {
    type: "email.complained";
    data: EmailComplainedData;
};
/** Narrow a verified event to `domain.sending_verified` with typed `data`. */
export declare function isDomainSendingVerified(e: WebhookEvent): e is WebhookEvent & {
    type: "domain.sending_verified";
    data: DomainSendingVerifiedData;
};
/** Narrow a verified event to `domain.sending_failed` with typed `data`. */
export declare function isDomainSendingFailed(e: WebhookEvent): e is WebhookEvent & {
    type: "domain.sending_failed";
    data: DomainSendingFailedData;
};
/** Narrow a verified event to `domain.suppression_added` with typed `data`. */
export declare function isDomainSuppressionAdded(e: WebhookEvent): e is WebhookEvent & {
    type: "domain.suppression_added";
    data: DomainSuppressionAddedData;
};
export interface ConstructEventOptions {
    toleranceSeconds?: number;
    now?: () => number;
}
/**
 * Verify a delivery and parse it into a typed {@link WebhookEvent} in one call
 * (Stripe's `constructEvent` shape). Throws {@link E2AWebhookSignatureError} on
 * a bad signature, a replay outside tolerance, or an unparseable body. This is
 * the recommended path — no separate verify step needed. Call it from your
 * webhook handler with the RAW request body.
 */
export declare function constructEvent(rawBody: string | Buffer, header: string, secret: string | string[], opts?: ConstructEventOptions): WebhookEvent;
export {};
