version: 1
name: synthetic
target:
  email: ${E2A_EVAL_PROBE_MISSING}
actor:
  email: ${E2A_EVAL_ACTOR}
transport:
  adapter: e2a
  api_key: ${E2A_EVAL_API_KEY}
  allowed_envelope_recipients: ["${E2A_EVAL_PROBE_MISSING}", "${E2A_EVAL_ACTOR}"]
cases: [case.yaml]
