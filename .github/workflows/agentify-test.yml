# Tests for the agentify feedback-loop framework (plugins/e2a-labs/skills/agentify).
# Deterministic: script selftests + addon unit tests + static/YAML/config
# validation. No secrets, no network — runs on every PR that touches it.
name: agentify-test

on:
  push:
    paths: ["plugins/e2a-labs/skills/agentify/**", ".github/workflows/agentify-test.yml"]
  pull_request:
    paths: ["plugins/e2a-labs/skills/agentify/**", ".github/workflows/agentify-test.yml"]

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: "20"
      - name: Install PyYAML (for config validation)
        run: pip install --quiet pyyaml
      - name: Run the agentify test suite
        run: bash plugins/e2a-labs/skills/agentify/test/run.sh
