{
  "name": "@e2a/sdk",
  "version": "5.4.0",
  "description": "TypeScript SDK for e2a — email for AI agents",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./v1": {
      "types": "./dist/v1/index.d.ts",
      "import": "./dist/v1/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "test": "npm run typecheck && npm run test:unit && npm run test:types",
    "typecheck": "tsc --project tsconfig.test.json",
    "test:unit": "vitest run test/v1 --exclude '**/contract*.test.ts'",
    "test:coverage": "npm run typecheck && npm run test:unit -- --coverage && npm run test:types",
    "test:types": "tsc -p tsconfig.type-tests.json",
    "test:contract": "vitest run test/v1/contract.test.ts test/v1/contract-client.test.ts",
    "pretest:live": "rm -rf test/coverage/reports",
    "test:live": "vitest run test/e2e.test.ts test/e2e-*.test.ts",
    "coverage:gate": "node test/coverage/gate.mjs",
    "coverage:live": "npm run test:live; npm run coverage:gate",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "email",
    "agent",
    "e2a",
    "webhook",
    "ai"
  ],
  "author": "TokenCanopy <josh@tokencanopy.com>",
  "license": "Apache-2.0",
  "homepage": "https://e2a.dev",
  "repository": {
    "type": "git",
    "url": "https://github.com/tokencanopy/e2a",
    "directory": "sdks/typescript"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "ws": "^8.21.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "@types/ws": "^8.18.1",
    "@vitest/coverage-v8": "^4.1.10",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10",
    "yaml": "^2.9.0"
  }
}
